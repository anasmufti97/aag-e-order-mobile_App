import 'package:flutter/material.dart';

class UiConstants {
  static const BorderRadius buttonBorderRadius = BorderRadius.all(Radius.circular(20));
  static const BorderRadius textFieldBorderRadius = BorderRadius.all(Radius.circular(10));
  static const BorderRadius authenticationTextFieldBorderRadius = BorderRadius.all(Radius.circular(20));
}
